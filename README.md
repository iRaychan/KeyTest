# KeySuite V2.33

Full GitHub source built from V2.32. `config.js` is intentionally not included; preserve the existing deployment configuration.

## Upgrade

Use the V2.32 → V2.33 update patch or replace the complete source. No Supabase migration is required.

See `V233_CHANGES.txt` and `INSTALL_V233.txt`.
